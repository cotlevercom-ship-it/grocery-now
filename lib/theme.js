// Design tokens for Cot Lever's business-matching platform.
// Palette: dark, editorial-premium — matches the marketing banner imagery
// (deep red/black gradients, warm white text, a single red accent line).
export const theme = {
  ink: '#F3E9E4',       // primary text on dark surfaces (warm off-white)
  inkSoft: '#C9AFA8',   // secondary/muted text on dark backgrounds
  paper: '#170A09',     // page background — near-black with a red cast
  surface: '#241211',   // card background — one step up from page bg
  line: '#3D211E',      // hairline borders on dark surfaces
  lineSoft: '#2C1615',
  brass: '#B3372A',     // primary accent — CTAs, price, emphasis (was gold, now banner red)
  brassDark: '#8A2A20',
  signal: '#4E9E72',    // verified/active/success states — kept legible on dark bg
  signalSoft: '#16241C',
  danger: '#E2503C',
  dangerSoft: '#2E1613',

  fontDisplay: "var(--font-fraunces), Georgia, serif",
  fontBody: "var(--font-plex-sans), Arial, sans-serif",
  fontMono: "var(--font-plex-mono), 'Courier New', monospace",
}
// redeploy trigger
// redeploy trigger
// redeploy trigger
// redeploy trigger
// redeploy trigger
// redeploy trigger
// redeploy trigger
// redeploy trigger 1787164945
// redeploy trigger 1787165520
